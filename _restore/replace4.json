{
    "old":  "            var usedH = 0;\n            var grid = card.querySelector(\".lane-tv-grid\");\n            if (grid instanceof HTMLElement) {\n                var panels = grid.querySelectorAll(\".lane-tv-panel:not(.lane-tv-panel--ring)\");\n                for (var pi = 0; pi \u003c panels.length; pi++) {\n                    var p = panels[pi];\n                    if (!(p instanceof HTMLElement)) continue;\n                    var bottom = p.offsetTop + p.offsetHeight;\n                    if (bottom \u003e usedH) usedH = bottom;\n                }\n            }\n\n            var vmin = Math.min(window.innerWidth || availW, window.innerHeight || availH);\n            var maxW = Math.floor(availW * 0.45);\n            var maxH = Math.floor((availH - usedH) * 0.85);\n            var target = Math.floor(Math.min(vmin * 0.38, maxW, maxH));\n            var size = target;\n            if (!isFinite(size) || size \u003c 150) size = 150;\n            if (size \u003e 650) size = 650;",
    "new":  "            var usedH = 0;\n            var toolbar = card.querySelector(\".lane-tv-toolbar\");\n            var hintBar = card.querySelector(\".lane-tv-hint-bar\");\n            if (toolbar instanceof HTMLElement) usedH += toolbar.offsetHeight;\n            if (hintBar instanceof HTMLElement) usedH += hintBar.offsetHeight;\n\n            var vmin = Math.min(window.innerWidth || availW, window.innerHeight || availH);\n            var maxW = Math.floor(availW * 0.58);\n            var maxH = Math.floor((availH - usedH) * 0.92);\n            var target = Math.floor(Math.min(vmin * 0.52, maxW, maxH));\n            var size = target;\n            if (!isFinite(size) || size \u003c 180) size = 180;\n            if (size \u003e 900) size = 900;"
}
