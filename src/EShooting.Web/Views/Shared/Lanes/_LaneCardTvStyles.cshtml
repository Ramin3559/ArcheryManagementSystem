<style id="lane-tv-styles-v3-24">
    /* TV zolaq kartı — layout v3 (MiBox / Android TV overscan nəzərə alınır) */
    .lane-tv-body .lane-card.is-cleanup .lane-tv-athlete {
        display: none !important;
    }

    .lane-tv-body .lane-tv-status-pill,
    .lane-tv-body .lane-card--tv .status-pill,
    .lane-tv-body .lane-tv-connection,
    .lane-tv-body #connectionBadge {
        display: none !important;
        visibility: hidden !important;
        pointer-events: none !important;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] {
        position: relative;
        background: var(--bg, #fff);
        border: none;
        border-radius: 0;
        padding: 0;
        width: 100%;
        height: 100%;
        overflow: visible;
        color: var(--text, #8B2318);
        box-sizing: border-box;
        --tv-row-font: clamp(46px, 5.4vmin, 66px);
        --tv-center-nudge: clamp(28px, 4vw, 56px);
        --tv-schedule-block-width: clamp(260px, 24vw, 380px);
        --tv-name-schedule-gap: clamp(4px, 0.5vw, 8px);
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-grid {
        display: grid !important;
        position: relative;
        width: 100%;
        height: 100%;
        min-height: 0;
        grid-template-columns: minmax(0, 1fr) minmax(0, 1fr) minmax(0, 1fr) !important;
        column-gap: 0 !important;
        row-gap: 0 !important;
        align-content: stretch !important;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"]:not(.lane-card--tv-single) .lane-tv-grid {
        grid-template-rows: auto auto 1fr !important;
        grid-template-areas:
            "logo   stack  clock"
            "name   times  ."
            ".      .      ." !important;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"].lane-card--tv-single .lane-tv-grid {
        grid-template-rows: auto auto auto 1fr !important;
        grid-template-areas:
            "logo   stack  clock"
            "detail detail detail"
            "detail detail detail"
            ".      .      ." !important;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"].lane-card--tv-single.lane-card--tv-vip .lane-tv-detail-grid {
        grid-template-rows: auto auto;
        grid-template-areas:
            "fname start ."
            "lname .     .";
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-detail-grid {
        grid-area: detail;
        grid-column: 1 / -1;
        grid-row: 2 / 4;
        display: grid;
        grid-template-columns: minmax(0, 1fr) minmax(0, 1fr) minmax(0, 1fr);
        grid-template-rows: auto auto;
        grid-template-areas:
            "fname start ."
            "lname end   .";
        column-gap: clamp(4px, 0.8vw, 10px);
        row-gap: clamp(8px, 1.2vh, 14px);
        padding-top: clamp(10px, 1.6vh, 20px);
        z-index: 2;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--name-first,
    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--name-last {
        grid-area: fname;
        justify-self: start;
        align-self: center;
        position: relative;
        z-index: 2;
        width: 100%;
        max-width: 100%;
        min-width: 0;
        overflow: visible;
        box-sizing: border-box;
        padding-right: 0;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--name-last {
        grid-area: lname;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--session-start,
    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--session-end {
        justify-self: center;
        align-self: center;
        width: var(--tv-schedule-block-width, clamp(260px, 24vw, 380px));
        max-width: 100%;
        text-align: left;
        pointer-events: none;
        transform: translateX(var(--tv-center-nudge, 28px));
        z-index: 3;
        box-sizing: border-box;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--session-start .lane-tv-center-col,
    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--session-end .lane-tv-center-col {
        width: 100%;
        text-align: left;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--session-start .lane-tv-center-inner,
    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--session-end .lane-tv-center-inner {
        display: block;
        width: 100%;
        text-align: left;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--session-start {
        grid-area: start;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--session-end {
        grid-area: end;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--session-start .lane-tv-schedule__item,
    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--session-end .lane-tv-schedule__item {
        display: block;
        font-size: var(--tv-row-font);
        font-weight: 700;
        color: #8B2318;
        font-variant-numeric: tabular-nums;
        white-space: nowrap;
        text-align: left;
        line-height: 1.08;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"]:not(.lane-card--tv-active) .lane-tv-grid {
        grid-template-rows: auto 1fr !important;
        grid-template-areas:
            "logo   stack  clock"
            ".      .      ." !important;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel {
        min-width: 0;
        min-height: 0;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--logo {
        grid-area: logo;
        align-self: start;
        justify-self: start;
        position: relative;
        z-index: 3;
    }

    /* Zolaq + Başlama/Bitmə — ekranın üfüqi mərkəzi (Zolaq ilə eyni xətt) */
    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--center-stack,
    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--session-times {
        justify-self: center;
        align-self: start;
        width: max-content;
        max-width: 100%;
        text-align: center;
        pointer-events: none;
        transform: translateX(var(--tv-center-nudge, 28px));
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--center-stack {
        grid-area: stack;
        grid-column: 1 / -1;
        grid-row: 1;
        padding-top: clamp(8px, 1.8vh, 24px);
        z-index: 1;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--session-times {
        grid-area: times;
        grid-column: 1 / -1;
        grid-row: 2;
        padding-top: clamp(10px, 1.6vh, 20px);
        z-index: 1;
        width: var(--tv-schedule-block-width, clamp(260px, 24vw, 380px));
        max-width: 100%;
        text-align: left;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--session-times .lane-tv-center-col,
    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--session-times .lane-tv-center-inner {
        width: 100%;
        text-align: left;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-center-col {
        display: block;
        width: 100%;
        max-width: 100%;
        text-align: center;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-center-inner {
        display: inline-block;
        text-align: left;
        vertical-align: top;
        max-width: min(100%, calc(100% - 2 * var(--tv-safe-x, 20px)));
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--clock {
        grid-area: clock;
        justify-self: end;
        align-self: start;
        padding-top: clamp(8px, 1.8vh, 24px);
        padding-right: 0;
        position: relative;
        z-index: 3;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-wall-clock__value {
        display: block;
        font-size: clamp(50px, 7vmin, 72px);
        font-weight: 800;
        font-variant-numeric: tabular-nums;
        color: #8B2318;
        line-height: 1.1;
        white-space: nowrap;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--name {
        grid-area: name !important;
        grid-row: 2;
        grid-column: 1;
        justify-self: start;
        align-self: start;
        width: 100%;
        max-width: 100%;
        padding-top: clamp(10px, 1.6vh, 20px);
        overflow: visible;
        position: relative;
        z-index: 2;
        min-width: 0;
        box-sizing: border-box;
        padding-right: 0;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--ring {
        position: absolute;
        right: 0;
        bottom: clamp(2px, 0.5vh, 8px);
        z-index: 2;
        display: flex;
        justify-content: flex-end;
        align-items: flex-end;
        overflow: visible;
        min-width: 0;
        min-height: min(380px, 48vh);
        max-width: 100%;
        padding: 0;
        margin-right: 0;
        pointer-events: none;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--center,
    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--zolaq,
    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--meta,
    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--hint,
    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--athlete,
    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--schedule,
    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-panel--info {
        display: none !important;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-brand-logo {
        display: block;
        max-height: clamp(96px, 16vh, 160px);
        max-width: clamp(180px, 22vmin, 290px);
        width: auto;
        height: auto;
        object-fit: contain;
        object-position: left top;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-lane-num {
        margin: 0;
        font-size: clamp(66px, 8.5vmin, 90px);
        font-weight: 800;
        color: #8B2318;
        text-align: left;
        line-height: 1.05;
        white-space: nowrap;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-athlete {
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        gap: clamp(4px, 0.8vh, 10px);
        width: 100%;
        max-width: 100%;
        min-width: 0;
        overflow: visible;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-athlete--group {
        gap: clamp(10px, 1.4vh, 18px);
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-person {
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        gap: clamp(2px, 0.35vh, 6px);
        max-width: 100%;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-athlete--group .lane-tv-first-name,
    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-athlete--group .lane-tv-last-name {
        font-size: var(--tv-row-font);
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-first-name,
    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-last-name {
        display: block;
        font-weight: 700;
        color: #8B2318;
        line-height: 1.08;
        white-space: nowrap;
        max-width: 100%;
        overflow: visible;
        font-size: var(--tv-row-font);
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-schedule {
        display: flex;
        flex-direction: column;
        gap: clamp(8px, 1.2vh, 14px);
        width: 100%;
        align-items: flex-start;
        margin-top: 0;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-schedule__row {
        display: flex;
        align-items: center;
        justify-content: flex-start;
        width: 100%;
        font-size: var(--tv-row-font);
        font-weight: 700;
        color: #8B2318;
        font-variant-numeric: tabular-nums;
        white-space: nowrap;
        text-align: left;
        line-height: 1.08;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-schedule__item {
        display: block;
        text-align: left;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-schedule__icon {
        display: none !important;
    }

    .lane-card.lane-card--tv[data-tv-layout="v3"] .lane-tv-equip {
        margin-top: 6px;
        font-size: clamp(28px, 3vw, 40px);
        line-height: 1;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .lane-bottom-hint {
        position: absolute;
        left: 50%;
        bottom: clamp(12px, 2vh, 28px);
        transform: translateX(-50%);
        font-size: clamp(32px, 3.8vmin, 46px) !important;
        font-weight: 700;
        color: #c81e1e;
        opacity: 0;
        transition: opacity 0.2s ease;
        pointer-events: none;
        line-height: 1.25;
        white-space: nowrap;
        text-align: center;
        z-index: 2;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .lane-bottom-hint.is-show {
        opacity: 1;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .lane-bottom-hint.is-critical {
        color: #b91c1c;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring-wrap {
        --ring-track: rgba(139, 35, 24, 0.14);
        --ring-accent: #8B2318;
        --ring-glow: rgba(139, 35, 24, 0.2);
        --ring-size: min(660px, 52vmin, 54vh);
        --ring-inner-pad: 0.03;
        position: relative;
        width: var(--ring-size) !important;
        height: var(--ring-size) !important;
        min-width: 200px;
        min-height: 200px;
        aspect-ratio: 1;
        max-width: 100%;
        max-height: 100%;
        margin: 0;
        background: transparent;
        border: 0;
        border-radius: 50%;
        padding: 0;
        box-shadow: none;
        overflow: visible;
        box-sizing: border-box;
        flex: 0 0 auto;
        transform: none !important;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring__svg {
        display: block;
        width: 100%;
        height: 100%;
        transform: rotate(-90deg);
        filter: drop-shadow(0 0 6px var(--ring-glow));
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring__track {
        fill: none;
        stroke: var(--ring-track);
        stroke-width: 5;
        stroke-linecap: butt;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring-wrap.is-open-ended .countdown-ring__progress {
        opacity: 0 !important;
        transition: none !important;
        stroke-dashoffset: 0 !important;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring-wrap.is-open-ended .countdown-ring__track {
        stroke: var(--ring-accent);
        stroke-width: 6;
        opacity: 1 !important;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring-wrap.is-open-ended.is-live .countdown-ring__timer {
        color: #8B2318;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring__progress {
        fill: none;
        stroke: var(--ring-accent);
        stroke-width: 6;
        stroke-linecap: round;
        transition: stroke-dasharray 0.35s ease, stroke-dashoffset 0.35s ease, stroke 0.3s ease;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring-wrap.is-near-full .countdown-ring__track {
        opacity: 0;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring__center {
        position: absolute;
        inset: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        padding: calc(var(--ring-inner-pad, 0.03) * 100%);
        pointer-events: none;
        overflow: hidden;
        box-sizing: border-box;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring__timer-shell {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 100%;
        max-width: 100%;
        overflow: hidden;
        box-sizing: border-box;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring__timer {
        display: block;
        font-size: clamp(78px, 8.5vmin, 120px);
        font-weight: 900;
        font-variant-numeric: tabular-nums;
        line-height: 1;
        color: #8B2318;
        white-space: nowrap;
        transform-origin: center center;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring-wrap.is-live .countdown-ring__timer {
        color: #8B2318;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring-wrap[data-time-wide="1"]:not(.is-idle):not(.is-done) .countdown-ring__timer {
        letter-spacing: -0.04em;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring__caption {
        display: none !important;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring__bartrack {
        display: none;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring-wrap.is-pending {
        --ring-accent: #c45a00;
        --ring-glow: rgba(196, 90, 0, 0.3);
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring-wrap.is-live.is-warn:not(.is-critical) {
        --ring-accent: #c45a00;
        --ring-glow: rgba(196, 90, 0, 0.35);
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring-wrap.is-live.is-critical {
        --ring-accent: #b91c1c;
        --ring-glow: rgba(185, 28, 28, 0.4);
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring-wrap.is-live.is-critical .countdown-ring__timer {
        color: #b91c1c;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring-wrap.is-live.is-critical .countdown-ring__svg {
        animation: lane-tv-ring-pulse-v3 0.95s ease-in-out infinite;
    }

    @@keyframes lane-tv-ring-pulse-v3 {
        0%, 100% { filter: drop-shadow(0 0 6px var(--ring-glow)); }
        50% { filter: drop-shadow(0 0 18px rgba(185, 28, 28, 0.55)); }
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring-wrap.is-done,
    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring-wrap.is-idle {
        --ring-accent: rgba(139, 35, 24, 0.35);
        --ring-glow: transparent;
        --ring-size: min(240px, 30vmin, 36vh);
        min-width: 140px;
        min-height: 140px;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring-wrap.is-idle .countdown-ring__timer {
        font-size: 58px !important;
        color: #8B2318;
    }

    .lane-tv-body .lane-card[data-tv-layout="v3"] .countdown-ring-wrap.is-done .countdown-ring__timer {
        font-size: 16px !important;
        white-space: normal;
        text-align: center;
        line-height: 1.25;
        padding: 0 8px;
        max-width: 92%;
        color: #8B2318;
    }
</style>
