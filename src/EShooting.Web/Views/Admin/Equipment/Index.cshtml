@using EShooting.Application.Common.Models
@using EShooting.Web.Helpers
@model IReadOnlyCollection<EquipmentCatalogItem>
@{
    Layout = "~/Views/Admin/_AdminLayout.cshtml";
    ViewData["Title"] = "Avadanlıqlar";
    var notice = TempData["EquipmentNotice"] as string;
    var error = TempData["EquipmentError"] as string;
}

<style>
    .notice { padding: 10px 12px; border-radius: 12px; margin-bottom: 12px; font-weight: 700; }
    .notice--ok { background: #ecfdf5; color: #166534; border: 1px solid rgba(22,163,74,0.25); }
    .notice--err { background: #fef2f2; color: #991b1b; border: 1px solid rgba(220,38,38,0.25); }
    .actions { display: flex; gap: 8px; flex-wrap: nowrap; align-items: center; }
</style>

<div class="topbar">
    <h1>Avadanlıqlar</h1>
    <div style="display:flex; align-items:center; gap:10px;">
        <button type="button" class="btn" id="adminRefreshBtn" title="Siyahını yenilə"><svg class="btn-ico" aria-hidden="true"><use href="#ai-refresh"/></svg><span class="btn-label">Yenilə</span></button>
        <a class="btn btn-excel" href="/admin/equipment/export.xlsx"><svg class="btn-ico" aria-hidden="true"><use href="#ai-excel"/></svg><span class="btn-label">Excel-ə çıxart</span></a>
        <a class="btn" href="/admin/equipment/history"><svg class="btn-ico" aria-hidden="true"><use href="#ai-journal"/></svg><span class="btn-label">Jurnal / hesabat</span></a>
        <a class="btn btn-primary" href="/admin/equipment/new"><svg class="btn-ico" aria-hidden="true"><use href="#ai-plus"/></svg><span class="btn-label">Yeni avadanlıq</span></a>
    </div>
</div>

@if (!string.IsNullOrWhiteSpace(notice))
{
    <div class="notice notice--ok">@notice</div>
}
@if (!string.IsNullOrWhiteSpace(error))
{
    <div class="notice notice--err">@error</div>
}

<div class="card">
    <div class="tableWrap">
        <table>
            <thead>
            <tr>
                <th>Ad</th>
                <th>Kateqoriya</th>
                <th>Anbar</th>
                <th>Zal</th>
                <th>Satış</th>
                <th>Cəmi</th>
                <th>Xarab</th>
                <th>Alış</th>
                <th>Satış qiyməti</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            @if (Model.Count == 0)
            {
                <tr><td colspan="10">Hələ avadanlıq yoxdur.</td></tr>
            }
            else
            {
                foreach (var item in Model)
                {
                    <tr>
                        <td><strong>@item.Name</strong></td>
                        <td>@EquipmentDisplayHelper.FormatCategory(item.Category)</td>
                        <td>@item.WarehouseQuantity ədəd</td>
                        <td>@item.RentalQuantity ədəd</td>
                        <td>@item.SaleQuantity ədəd</td>
                        <td><strong>@item.Quantity ədəd</strong></td>
                        <td>@(item.DamagedQuantity > 0 ? $"{item.DamagedQuantity} ədəd" : "—")</td>
                        <td>@EquipmentDisplayHelper.FormatPrice(item.PurchasePrice)</td>
                        <td>@EquipmentDisplayHelper.FormatUnitPrice(item)</td>
                        <td>
                            <div class="actions">
                                <a class="btn btn-primary" href="/admin/equipment/@item.Id/edit"><svg class="btn-ico" aria-hidden="true"><use href="#ai-edit"/></svg><span class="btn-label">Dəyişdir</span></a>
                                <form method="post" action="/admin/equipment/@item.Id/delete" style="margin:0;" onsubmit="return confirm('Silinsin?');">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-danger"><svg class="btn-ico" aria-hidden="true"><use href="#ai-trash"/></svg><span class="btn-label">Sil</span></button>
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            }
            </tbody>
        </table>
    </div>
</div>

<script>
    document.getElementById("adminRefreshBtn")?.addEventListener("click", function () {
        window.location.reload();
    });
</script>
